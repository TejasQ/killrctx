# Tasks — Outline Visual Theme

## Tasks

- [x] TASK-01: [UI] Add `outlineComponents` constant in `src/app/notebooks/[id]/page.tsx`
      immediately after `markdownComponents`
- [x] TASK-02: [UI] In `NoteCard`, swap to `outlineComponents` when `note.type === "outline"`
- [x] TASK-03: [UI] In `StudioPanel` expanded reading view, swap to `outlineComponents`
      when `expandedNote.type === "outline"`
- [x] TASK-04: [API] Sharpen the `outline` entry in `NOTE_PROMPTS` in
      `src/app/api/notebooks/[id]/notes/[noteId]/route.ts`
