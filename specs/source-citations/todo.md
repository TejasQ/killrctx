# Task Plan — Source Citations in Chat

## Tasks

- [x] TASK-01: [UI] Extend `fixMarkdown()` in `page.tsx` to strip LLM tool-call narration
- [x] TASK-02: [API] Forward `SourcesEvent` as a `"sources"` SSE event in `chat/route.ts`
- [x] TASK-03: [UI] Add new `src/components/SourceCitation.tsx` component
- [x] TASK-04: [UI] Add `messageSources` state + `deduplicateSources` helper + wire `SourceCitation` into `ChatPanel`
